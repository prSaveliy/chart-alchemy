{
  "name": "chart-alchemy",
  "private": true,
  "version": "1.0.0",
  "description": "Web application for data visualization using LLM",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "db:up": "npm run db:up --workspace=backend",
    "db:down": "npm run db:down --workspace=backend",
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "dev:test": "npm run dev --workspace=frontend & npm run dev:test --workspace=backend",
    "start": "npm run start --workspace=backend",
    "start:test": "npm run start:test --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend",
    "lint:fix": "npm run lint:fix --workspace=frontend && npm run lint:fix --workspace=backend",
    "format": "npm run format --workspace=backend",
    "format:check": "npm run format:check --workspace=backend",
    "test": "npm run test --workspace=backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/prSaveliy/chart-alchemy.git"
  },
  "author": "Prysiazhnyi Savelii <papsavel@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/prSaveliy/chart-alchemy/issues"
  },
  "homepage": "https://github.com/prSaveliy/chart-alchemy#readme"
}
